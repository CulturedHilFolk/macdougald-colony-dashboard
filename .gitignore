# macOS
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Python
__pycache__/
*.pyc

# Raw colony exports — these come from the colony software, treat as source data
# (kept local; not pushed to GitHub)
CageListExcel.xlsx
MacLab - Brian_Mice.xlsx
StrainList.xlsx
Breedings.xlsx
Mouse tracking sheet.xlsx

# Generated rack walkthrough templates (printable forms, regenerable from script)
Rack_Template_*.xlsx
Cage_Walkthrough_*.xlsx

# Filled walkthrough scans / digitized observed inventory (sensitive — keep local)
Walkthrough_Filled_*.xlsx
Observed_Inventory_*.xlsx

# Transnetyx submission / upload files (contain mouse-level metadata — keep local)
Transnetyx_Submission_*.xlsx
*_Transnetyx_Upload_*.xlsx
MacLab_Brian_Mice_*.xlsx

# OS / editor cruft
*.swp
*.swo
.vscode/

# (The old prototype dashboard files have been retired and replaced with index.html
#  + data/colony.json — see README. The notebook will be re-pointed at the new pipeline.)
